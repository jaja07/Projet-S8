@model IEnumerable<TestS8.Models.Simulation>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Utilisateur)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Utilisateur.UtilisateurID)
            </td>
            <td>
                <a asp-controller="Modeles" asp-action="IndexSimul" asp-route-id="@item.SimulationID">Afficher</a> |
                <a asp-action="Details" asp-route-id="@item.SimulationID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SimulationID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
