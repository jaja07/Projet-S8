@model IEnumerable<TestS8.Models.Parametres>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Valeur)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Modeles)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Valeur)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Modeles.ModeleID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ParametresID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ParametresID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ParametresID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
